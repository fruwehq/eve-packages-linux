#!/usr/bin/env sh
set -eu

exec ./scripts/package-down-command sunshine 'eve_human_run systemctl --user disable --now sunshine || true; sudo systemctl disable --now sunshine || true; sudo apt-get remove -y sunshine || true'
