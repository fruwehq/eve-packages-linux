#!/usr/bin/env sh
set -eu

exec ./scripts/package-down-command docker 'eve_human_run systemctl --user disable --now docker || true; eve_human_run dockerd-rootless-setuptool.sh uninstall || true; sudo systemctl disable --now docker.service docker.socket || true; sudo apt-get remove -y docker-ce docker-ce-cli docker-ce-rootless-extras docker-buildx-plugin docker-compose-plugin slirp4netns uidmap || true'
